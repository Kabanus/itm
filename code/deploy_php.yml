# Deploy PHP
---
- name: Deploy PHP
  hosts: all

  tasks:
    
    # Deploy PHP
    - name: PHP - install packages
      apt:
        name: '{{ packages }}'
        state: latest
        install_recommends: yes
      vars:
        packages:
        - php-fpm
        - php-mysql
